#!/bin/bash

filepath="/$HOME/derick/druid/1009-test/druid/tests/pta/*"

for d in $filepath 
do
if [ "$d"!='utils' ]; then 
    newline="/home/derick/druid/1009-test/druid/tests/pta/$d/*"
    echo $newline
    echo "ok"
for f in newline
do 
       # if [ "$f" == *.java ]; then
python test.py "$f"
	#fi
done
fi
done
    
#/home/derick/druid1009-test/druid/tests/pta/*
